# CLAUDE.md

## 프로젝트

- Kotlin + Spring Boot + Spring Data JPA (H2)
- 첫 사이드 프로젝트. 과한 추상화보다 **단순함 우선**.

## 아키텍처 결정

**레이어드 아키텍처를 쓴다. 헥사고날은 도입하지 않는다.**

- 구조: Controller → Service → Repository
- 헥사고날(포트/어댑터)은 이 프로젝트엔 오버킬. 나중에 진짜 필요해지면 그때 고민한다.

### 핵심 원칙: 도메인은 인프라를 모른다

"레이어드라서 도메인 테스트가 안 된다"는 건 오해다. 레이어링 자체가 테스트를 막는 게 아니라,
**도메인에 프레임워크/DB가 새어 들어올 때** 막힌다. 그래서 한 가지만 지킨다:

> 비즈니스 규칙(도메인 로직)은 순수 객체로 두고, 프레임워크·DB·외부 API를 모른다.

이것만 지키면 레이어드 구조에서도 도메인 단위 테스트가 된다.

- 상태 전이/검증 같은 비즈니스 규칙은 엔티티/도메인 객체 안의 순수 메서드로 둔다.
  → DB 없이 `Order(...).cancel()` 식으로 단위 테스트 가능.
- DB를 타는 통합 로직은 Service에서 `@DataJpaTest` / `@SpringBootTest`로 따로 검증한다.

### 테스트 가능성의 진짜 변수

"레이어드냐 헥사고날이냐"가 아니라 **"도메인 로직이 인프라에 의존하느냐"**다.
도메인을 순수하게 유지하는 한 레이어드로 충분하다.
